apply plugin: 'com.android.application'

android {
    defaultConfig {
        ndk {
            abiFilters "armeabi-v7a"
            stl = "stlport_static"
            cFlags "-fexceptions"
            ldLibs "log"
        }
    }
}
